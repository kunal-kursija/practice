services:
  # Service For Converting Parameters.
  practice.site_api_key_service:
    # Class callback.
    class: Drupal\practice\ParamConverter\SiteApiKeyConverter
    arguments: ['@config.factory']
    tags:
      - { name: paramconverter }
